<?xml version="1.0" encoding="iso-8859-1"?>

<!-- 
  Please see the license.html file included with this distribution for 
  attribution and copyright information.
-->

<root>
	<windowclass name="reference_weapontable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "weapon", "reference.weapon@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>
	<windowclass name="reference_armortable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "armor", "reference.armor@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>
	<windowclass name="reference_adventuringgeartable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "gear", "reference.gear@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>
	<windowclass name="reference_mountsandotheranimalstable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "vehiclemount", "reference.vehiclemount@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>
	<windowclass name="reference_waterbornevehiclestable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "vehiclewater", "reference.vehiclewater@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>
</root>
